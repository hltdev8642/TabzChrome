# TabzChrome Beads Configuration
# Native beads workflow - simple issue tracking across sessions

# Standard workflow:
#   bd ready              # Find available work
#   bd show <id>          # Get context for an issue
#   bd update <id> --status in_progress  # Claim it
#   # ... do the work ...
#   bd close <id> --reason "what you did"
#   bd sync && git push   # Persist

# Labels for categorization:
#   feature, bug, chore, task, docs

# Custom statuses (if needed):
#   awaiting_review   - Work complete, needs review
#   awaiting_merge    - Review passed, ready to merge
